import React from 'react';
import { CssBaseline, Grid} from '@material-ui/core';


import Header from './Components/Header/Header';
import List from './Components/List/List';
import Map from './Components/Map/Map';

const App = ()=> {
    return (
        <>
            {/* <CssBaseline/> */}
            {/* <Header />
            <Grid container spacing={3} style={{width: '100%'}}>
                {/* This means that the grid would have 2 properties, (xs=12 spaces), i.e it would take full width on mobile devices and (md=4) i.e it would take only 4spaces on medium devices and larger */}
                {/* <Grid item xs={12} md={4}>
                    <List />
                </Grid>
                {/* The map would take only 8 spaces on medium or larger devices (md=8) */}
                {/* <Grid item xs={12} md={8}>
                    <Map />
                </Grid>
            </Grid> */} */}
        </>
    ); */}
}

export default App;